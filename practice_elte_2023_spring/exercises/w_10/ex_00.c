// Can the enum data be entered by the user ??? Explain.

// No, enums are inherently constant and cannot be changed.
// Not after its initialization, and by extension, "the user".

int main()
{
    return 0;
}